function addTodo(){
    const inputElement = document.querySelector("input");
    const value = inputElement.value;
    console.log(value);
    // in same way we can use getEkementById and getElementByClass if we wnt to get any tag or element by its class or id.
}